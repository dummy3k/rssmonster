from rssmonster.tests import *

class TestBayesController(TestController):

    def test_index(self):
        pass
        #response = self.app.get(url(controller='bayes', action='index'))
        # Test response...

    def test_rss_report(self):
        #response = self.app.get(url(controller='bayes', action='mixed_rss_with_report', user_id=1, id=1))
        self.assertEqual(1,1)
        pass
        # Test response...
